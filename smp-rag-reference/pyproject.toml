[build-system]
requires=["hatchling"]
build-backend="hatchling.build"
[project]
name="smp-rag-reference"
version="0.1.0"
description="Secure, citation-first self-service RAG reference implementation"
readme="README.md"
license={text="MIT"}
requires-python=">=3.10"
dependencies=[]
[project.optional-dependencies]
dev=["pytest>=8","ruff>=0.8"]
[tool.hatch.build.targets.wheel]
packages=["src/smp_rag"]
